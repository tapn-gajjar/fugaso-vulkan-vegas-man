import {MobileKeyWBase} from "../../../casino/crashGames/gui/windows/MobileKeyWBase";

export default class MobileKeyW extends MobileKeyWBase {
    constructor() {
        super();
    }

    _createElements() {
        super._createElements();
    }

    _clearElements() {
        super._clearElements();
    }

    _windowSizeUpdate() {
        super._windowSizeUpdate();
    }
}
